import { SimpleIfDirective } from './simple-if.directive';

describe('SimpleIfDirective', () => {
  it('should create an instance', () => {
    const directive = new SimpleIfDirective();
    expect(directive).toBeTruthy();
  });
});
